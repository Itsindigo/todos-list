<%= link_to 'Create Todo', new_todo_path %>

<ul class="todos">
  <% @todos.each do |todo| %>
    <li class="<%= "completed" if todo.completed? %>">
      <%= todo.title %>
      <%= button_to "Todone!", todo_completion_path(todo) %>
    </li>
  <% end %>
</ul>
